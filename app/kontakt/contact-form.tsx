"use client";
import { FormEvent, useState } from "react";
export default function ContactForm() { const [state, setState] = useState<"idle" | "sending" | "success" | "error">("idle"); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); setState("sending"); try { const response = await fetch("/api/inquiries/project-request", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: data.get("name"), company: data.get("company"), email: data.get("email"), phone: data.get("phone"), budget: data.get("budget"), description: data.get("description"), projectType: "Sonstiges", timeline: "flexibel", privacy: true, website: "" }) }); if (!response.ok) throw new Error(); event.currentTarget.reset(); setState("success"); } catch { setState("error"); } } return <form className="contact-form contact-page-form" onSubmit={submit}><label>Name<input name="name" required minLength={2} placeholder="Dein Vor- und Nachname" /></label><label>Firma<input name="company" placeholder="Name deines Unternehmens" /></label><label>E-Mail<input name="email" type="email" required placeholder="name@unternehmen.de" /></label><label>Telefon<input name="phone" type="tel" required placeholder="+49 ..." /></label><label>Projektbudget<select name="budget" required defaultValue=""><option value="" disabled>Bitte auswählen</option><option>unter 500 €</option><option>500-1000 €</option><option>1000-3000 €</option><option>über 3000 €</option></select></label><label>Projektbeschreibung<textarea name="description" required minLength={20} rows={6} placeholder="Was möchtest du erreichen?" /></label>{state === "success" && <p className="contact-form-status">Danke, deine Anfrage wurde gespeichert. Ich melde mich zeitnah.</p>}{state === "error" && <p className="contact-form-status">Die Anfrage konnte nicht gespeichert werden. Bitte versuche es erneut.</p>}<button className="contact-submit" disabled={state === "sending"}>{state === "sending" ? "Wird gesendet..." : "Kostenloses Erstgespräch vereinbaren"} <span aria-hidden="true">↗</span></button></form>; }
